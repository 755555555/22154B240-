from data_analysis import all_data
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats = all_data.groupby('home_team').agg({
    'score_home': 'mean',
    'score_away': 'mean'
}).reset_index()
# ... 雷达图代码 ...
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 赛季热度趋势
plt.figure(figsize=(12,6))
sns.lineplot(data=all_data, x='date', y='score_home', hue='format')
plt.title('KPL赛季比赛热度趋势')
plt.savefig('season_trend.png')

# 2. 战队表现力雷达图
team_stats